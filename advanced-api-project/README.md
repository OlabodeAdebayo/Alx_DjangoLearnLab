### Query Parameters

- **Filtering**  
  `?title=<title>`  
  `?author__name=<author>`  
  `?publication_year=<year>`

- **Search**  
  `?search=<text>` (searches `title` and `author name`)

- **Ordering**  
  `?ordering=title` (asc)  
  `?ordering=-title` (desc)

